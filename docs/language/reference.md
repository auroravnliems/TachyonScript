# Standard library reference

<!-- Generated by `tys docs` from the standard library declarations. Do not edit by hand:
     run `./gradlew -q :tachyon-cli:run --args=docs > docs/language/reference.md`. -->

Everything below is checked by the compiler: using a name that is not listed here, or
with the wrong types, is a compile error with a suggestion.

* [Events](#events)
* [Global functions and properties](#global-functions-and-properties)
* [Types](#types)

## Events

A handler is written `event <name> { ... }`. The variables below are available in the
handler; `event` is the event object itself.

### `player.join`

A player joined the server.

| Variable | Type | Description |
|---|---|---|
| `player` | `Player` | The player who joined. |

`event` is a [`PlayerJoinEvent`](#playerjoinevent).

```tys
event player.join {
    player.send("<green>Welcome {player.name}!")
}
```

### `player.quit`

A player left the server.

| Variable | Type | Description |
|---|---|---|
| `player` | `Player` | The player who left. |

`event` is a [`PlayerQuitEvent`](#playerquitevent).

### `player.death`

A player died.

| Variable | Type | Description |
|---|---|---|
| `victim` | `Player` | The player who died. |
| `killer` | `Player?` | The player who killed them, if any. |

`event` is a [`PlayerDeathEvent`](#playerdeathevent).

### `player.chat`

A player sent a chat message.

Chat is handled off the main thread. Treat the message as untrusted text.

| Variable | Type | Description |
|---|---|---|
| `player` | `Player` | The player who sent the message. |
| `message` | `string` | The plain text of the message. |

`event` is a [`PlayerChatEvent`](#playerchatevent); the event can be cancelled with `event.cancel()`.

### `player.move`

A player moved or turned.

Fires very often (many times per second per player). Keep handlers short.

| Variable | Type | Description |
|---|---|---|
| `player` | `Player` | The moving player. |
| `from` | `Location` | Where the player was. |
| `to` | `Location` | Where the player is moving to. |

`event` is a [`PlayerMoveEvent`](#playermoveevent); the event can be cancelled with `event.cancel()`.

### `block.break`

A player is breaking a block.

| Variable | Type | Description |
|---|---|---|
| `player` | `Player` | The player breaking the block. |
| `block` | `Block` | The block being broken. |

`event` is a [`BlockBreakEvent`](#blockbreakevent); the event can be cancelled with `event.cancel()`.

### `entity.damage`

An entity is about to take damage. Change event.damage to modify it.

| Variable | Type | Description |
|---|---|---|
| `entity` | `Entity` | The damaged entity. |
| `cause` | `string` | Damage cause, e.g. 'fall' or 'entity_attack'. |

`event` is a [`EntityDamageEvent`](#entitydamageevent); the event can be cancelled with `event.cancel()`.

## Global functions and properties

### Top level

| Name | Type | Description |
|---|---|---|
| `broadcast(message: Component)` | `void` | Sends a message to every online player and the console. |
| `location(world: World, x: double, y: double, z: double)` | `Location` | Creates a location. |
| `log(message: string)` | `void` | Writes a message to the server log. |
| `log(message: any?)` | `void` | Writes a value to the server log. |

### `GameMode`

| Name | Type | Description |
|---|---|---|
| `GameMode.ADVENTURE` | `GameMode` | The adventure game mode. |
| `GameMode.CREATIVE` | `GameMode` | The creative game mode. |
| `GameMode.SPECTATOR` | `GameMode` | The spectator game mode. |
| `GameMode.SURVIVAL` | `GameMode` | The survival game mode. |

### `log`

| Name | Type | Description |
|---|---|---|
| `log.debug(message: string)` | `void` | Writes a debug message (only shown when debug is enabled). |
| `log.debug(message: any?)` | `void` | Writes a value as a debug message. |
| `log.error(message: string)` | `void` | Writes an error to the server log. |
| `log.error(message: any?)` | `void` | Writes a value as an error. |
| `log.info(message: string)` | `void` | Writes an information message to the server log. |
| `log.info(message: any?)` | `void` | Writes a value to the server log. |
| `log.warn(message: string)` | `void` | Writes a warning to the server log. |
| `log.warn(message: any?)` | `void` | Writes a value as a warning. |

### `math`

| Name | Type | Description |
|---|---|---|
| `math.abs(value: int)` | `int` | Absolute value. |
| `math.abs(value: long)` | `long` | Absolute value. |
| `math.abs(value: double)` | `double` | Absolute value. |
| `math.ceil(value: double)` | `int` | Smallest whole number not less than the value. |
| `math.clamp(value: int, min: int, max: int)` | `int` | The value limited to the range min..max. |
| `math.clamp(value: double, min: double, max: double)` | `double` | The value limited to the range min..max. |
| `math.cos(value: double)` | `double` | Cosine of an angle in radians. |
| `math.floor(value: double)` | `int` | Largest whole number not greater than the value. |
| `math.max(a: int, b: int)` | `int` | The larger value. |
| `math.max(a: long, b: long)` | `long` | The larger value. |
| `math.max(a: double, b: double)` | `double` | The larger value. |
| `math.min(a: int, b: int)` | `int` | The smaller value. |
| `math.min(a: long, b: long)` | `long` | The smaller value. |
| `math.min(a: double, b: double)` | `double` | The smaller value. |
| `math.pi` | `double` | The ratio of a circle's circumference to its diameter. |
| `math.pow(base: double, exponent: double)` | `double` | base raised to the power exponent. |
| `math.random()` | `double` | A random number from 0 (inclusive) to 1 (exclusive). |
| `math.randomInt(min: int, max: int)` | `int` | A random whole number from min to max, both inclusive. |
| `math.round(value: double)` | `int` | Nearest whole number (halves round up). |
| `math.sin(value: double)` | `double` | Sine of an angle in radians. |
| `math.sqrt(value: double)` | `double` | Square root. |
| `math.tan(value: double)` | `double` | Tangent of an angle in radians. |

### `server`

| Name | Type | Description |
|---|---|---|
| `server.maxPlayers` | `int` | The player limit. |
| `server.onlineCount` | `int` | Number of online players. |
| `server.player(name: string)` | `Player?` | The online player with this exact name, or null. |
| `server.players` | `List<Player>` | A snapshot of the online players. |
| `server.world(name: string)` | `World?` | The loaded world with this name, or null. |
| `server.worlds` | `List<World>` | The loaded worlds. |

### `text`

| Name | Type | Description |
|---|---|---|
| `text.escape(text: string)` | `string` | Escapes MiniMessage tags so that untrusted text is shown literally. |
| `text.mini(miniMessage: string)` | `Component` | Parses MiniMessage text into a component (tags are formatted). |
| `text.plain(component: Component)` | `string` | The plain text of a component, without formatting. |

## Types

Built into the language: `int`, `long`, `float`, `double`, `bool`, `Duration`, `List<T>`,
and `T?` (a `T` or `null`); see [Types](types.md).

### any

Any non-null value.

### string

Immutable text.

| Member | Type | Description |
|---|---|---|
| `contains(text: string)` | `bool` | Whether the text contains another text. |
| `endsWith(text: string)` | `bool` | Whether the text ends with a suffix. |
| `isEmpty` | `bool` | Whether the text is empty. |
| `length` | `int` | Number of characters. |
| `lower()` | `string` | Lower-case copy. |
| `replace(target: string, replacement: string)` | `string` | Copy with every occurrence of target replaced. |
| `startsWith(text: string)` | `bool` | Whether the text starts with a prefix. |
| `toDouble()` | `double?` | The text as a number, or null if it is not one. |
| `toInt()` | `int?` | The text as a whole number, or null if it is not one. |
| `trim()` | `string` | Copy without leading and trailing spaces. |
| `upper()` | `string` | Upper-case copy. |

### Component

Formatted chat text (MiniMessage).

### UUID

A unique identifier.

| Member | Type | Description |
|---|---|---|
| `toString()` | `string` | The canonical text form of the UUID. |

### CommandSender

Something that can run commands and receive messages: a player or the console.

| Member | Type | Description |
|---|---|---|
| `hasPermission(permission: string)` | `bool` | Whether the sender has a permission. |
| `name` | `string` | The sender's name (the player name, or CONSOLE). |
| `op` | `bool` | Whether the sender is a server operator. |
| `send(message: Component)` | `void` | Sends a chat message. |

### World

A loaded world.

| Member | Type | Description |
|---|---|---|
| `name` | `string` | The world's name. |
| `players` | `List<Player>` | A snapshot of the players currently in the world. |
| `time` (read/write) | `long` | Time of day in ticks (0 to 24000). |
| `toString()` | `string` | The world's name. |

### Location

A position (x, y, z, yaw, pitch) in a world.

| Member | Type | Description |
|---|---|---|
| `add(x: double, y: double, z: double)` | `Location` | A new location offset by the given amounts. |
| `block` | `Block` | The block at the location. |
| `distance(other: Location)` | `double` | Distance to another location in the same world. |
| `pitch` | `float` | Vertical rotation in degrees. |
| `toString()` | `string` | Text such as 'world 10.5, 64, -3'. |
| `world` | `World?` | The world of the location, or null if it is not loaded. |
| `x` | `double` | The x coordinate. |
| `y` | `double` | The y coordinate. |
| `yaw` | `float` | Horizontal rotation in degrees. |
| `z` | `double` | The z coordinate. |

### Entity

Any entity in a world.

| Member | Type | Description |
|---|---|---|
| `location` | `Location` | A copy of the entity's current location. |
| `name` | `string` | The entity's name. |
| `teleport(destination: Location)` | `void` | Teleports the entity to a location. |
| `teleport(target: Entity)` | `void` | Teleports the entity to another entity. |
| `uuid` | `UUID` | The entity's unique id. |
| `valid` | `bool` | Whether the entity still exists (false after it died, despawned or the player left). |
| `world` | `World` | The world the entity is in. |

### LivingEntity

An entity with health, such as a mob or a player.

Has all members of [`Entity`](#entity).

| Member | Type | Description |
|---|---|---|
| `health` (read/write) | `double` | Current health, between 0 and maxHealth. |
| `maxHealth` | `double` | Maximum health. |

### Player

An online player.

Has all members of [`LivingEntity`](#livingentity) and [`CommandSender`](#commandsender).

| Member | Type | Description |
|---|---|---|
| `displayName` (read/write) | `Component` | The name shown in chat. |
| `food` (read/write) | `int` | Food level, from 0 to 20. |
| `gameMode` (read/write) | `GameMode` | The player's game mode. |
| `kick(reason: Component)` | `void` | Disconnects the player with a reason. |
| `level` (read/write) | `int` | Experience level. |
| `toString()` | `string` | The player's name; used when a player is inserted into text. |

### Block

A block at a position in a world.

| Member | Type | Description |
|---|---|---|
| `location` | `Location` | The block's location. |
| `type` | `string` | The block's material key, e.g. 'minecraft:stone'. |
| `world` | `World` | The block's world. |

### GameMode

A player game mode: GameMode.SURVIVAL, GameMode.CREATIVE, GameMode.ADVENTURE or GameMode.SPECTATOR.

| Member | Type | Description |
|---|---|---|
| `toString()` | `string` | Lower-case name, e.g. 'creative'. |

### Cancellable

An event that handlers can cancel.

| Member | Type | Description |
|---|---|---|
| `cancel()` | `void` | Cancels the event. |
| `cancelled` (read/write) | `bool` | Whether the event is cancelled. |
| `uncancel()` | `void` | Un-cancels the event. |

### PlayerJoinEvent

| Member | Type | Description |
|---|---|---|
| `joinMessage` (read/write) | `Component?` | The broadcast join message; null for none. |

### PlayerQuitEvent

| Member | Type | Description |
|---|---|---|
| `quitMessage` (read/write) | `Component?` | The broadcast quit message; null for none. |

### PlayerDeathEvent

| Member | Type | Description |
|---|---|---|
| `deathMessage` (read/write) | `Component?` | The broadcast death message; null for none. |
| `keepInventory` (read/write) | `bool` | Whether the player keeps their inventory. |

### PlayerChatEvent

Has all members of [`Cancellable`](#cancellable).

### PlayerMoveEvent

Has all members of [`Cancellable`](#cancellable).

### BlockBreakEvent

Has all members of [`Cancellable`](#cancellable).

### EntityDamageEvent

Has all members of [`Cancellable`](#cancellable).

| Member | Type | Description |
|---|---|---|
| `damage` (read/write) | `double` | The amount of damage. |

